function sumFirstAndLastElements(arrayOfNums) {

    let firstElement = arrayOfNums[0];
    let lastElement = arrayOfNums[arrayOfNums.length - 1];

    console.log(firstElement + lastElement);
}

sumFirstAndLastElements([20, 30, 40]);